# dotfiles
All my dotfiles and an installation script

install iTerm2

clone into home directory
sh makesymlinks.sh

note: this will install zshell if it isn't already installed!

set iterm2 colorschemes with schemes located within /itermcolors
Within VIM run :PluginInstall

set environment variables in .zprofile
